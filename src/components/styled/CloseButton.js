import styled from "styled-components";

const CloseButton = styled.button`
  background: black;
  position: absolute;
  top: 0;
  right: 0;
  width: 2rem;
  color: white;
  z-index: 2;
  border: 0;
`;

export default CloseButton;
